# Codekita - Website & UI/UX Design Services

Website modern untuk Codekita yang menawarkan layanan pengembangan website dan desain UI/UX profesional. Platform ini memungkinkan pengguna untuk menjelajahi layanan dan mengajukan pesanan melalui antarmuka yang bersih dan modern.

## 🚀 Fitur

- ✨ **Desain Modern**: Antarmuka yang menarik dan responsif menggunakan Framer Motion
- 📱 **Responsive Design**: Optimal di berbagai ukuran layar (desktop, tablet, mobile)
- 🎨 **UI/UX Design Services**: Layanan desain antarmuka profesional
- 💻 **Web Development Services**: Layanan pengembangan website modern dan SEO-friendly
- 📝 **Form Pemesanan**: Form untuk mengajukan pesanan layanan
- 📞 **Kontak Integrasi**: Integrasi dengan WhatsApp untuk komunikasi langsung

## 🛠️ Teknologi yang Digunakan

- **React** ^19.2.0 - Library JavaScript untuk membangun UI
- **TypeScript** ~5.8.2 - Superset JavaScript dengan type checking
- **Vite** ^6.2.0 - Build tool dan development server yang cepat
- **Framer Motion** ^12.23.24 - Library animasi untuk React
- **Lucide React** ^0.553.0 - Icon library
- **Tailwind CSS** - Utility-first CSS framework

## 📋 Prasyarat

Sebelum menjalankan proyek ini, pastikan Anda telah menginstall:

- **Node.js** (versi 16 atau lebih baru)
- **npm** atau **yarn** atau **pnpm**

## 🔧 Instalasi

1. Clone repository ini:
   ```bash
   git clone https://github.com/Kacong05/codekita.git
   cd codekita
   ```

2. Install dependencies:
   ```bash
   npm install
   ```

3. (Opsional) Buat file `.env.local` jika diperlukan untuk konfigurasi environment variables:
   ```env
   # Tambahkan environment variables jika diperlukan
   ```

## 🏃 Menjalankan Proyek

### Development Mode

Jalankan development server:

```bash
npm run dev
```

Aplikasi akan berjalan di `http://localhost:5173` (atau port yang ditampilkan di terminal).

### Build untuk Production

Build aplikasi untuk production:

```bash
npm run build
```

File yang sudah di-build akan tersimpan di folder `dist/`.

### Preview Production Build

Preview build production secara lokal:

```bash
npm run preview
```

## 📁 Struktur Proyek

```
codekita/
├── components/          # Komponen React
│   ├── Navbar.tsx      # Navigation bar
│   ├── Hero.tsx        # Hero section
│   ├── Services.tsx    # Services section
│   ├── OrderForm.tsx   # Order form component
│   ├── Contact.tsx     # Contact section
│   ├── Footer.tsx      # Footer component
│   └── icons/          # Icon components
├── App.tsx             # Main App component
├── constants.ts        # Constants dan konfigurasi
├── types.ts            # TypeScript type definitions
├── index.tsx           # Entry point
├── vite.config.ts      # Vite configuration
└── package.json        # Dependencies dan scripts
```

## 🤝 Kontribusi

Kontribusi selalu diterima! Jika Anda ingin berkontribusi pada proyek ini:

1. Fork repository ini
2. Buat branch untuk fitur Anda (`git checkout -b feature/AmazingFeature`)
3. Commit perubahan Anda (`git commit -m 'Add some AmazingFeature'`)
4. Push ke branch (`git push origin feature/AmazingFeature`)
5. Buka Pull Request

## 📝 Lisensi

Proyek ini adalah project pribadi. Silakan hubungi pemilik repository untuk informasi lebih lanjut tentang penggunaan dan lisensi.

## 📞 Kontak

Untuk informasi lebih lanjut tentang layanan Codekita, silakan hubungi melalui WhatsApp atau melalui form kontak di website.

---

⭐ Jika proyek ini membantu Anda, jangan lupa berikan star pada repository ini!
